import 'package:flutter/material.dart';

class ChangePinForm extends StatelessWidget {
  const ChangePinForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
